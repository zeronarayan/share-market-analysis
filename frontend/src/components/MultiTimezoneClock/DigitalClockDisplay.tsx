import { useMemo } from 'react';\n\ninterface Props {\n  currentTime: Date;\n  offset: number;\n}\n\nexport default function DigitalClockDisplay({ currentTime, offset }: Props) {\n  const timeString = useMemo(() => {\n    const offsetMs = offset * 3600000;\n    const timezoneTime = new Date(currentTime.getTime() + offsetMs);\n\n    const hours = String(timezoneTime.getUTCHours()).padStart(2, '0');\n    const minutes = String(timezoneTime.getUTCMinutes()).padStart(2, '0');\n    const seconds = String(timezoneTime.getUTCSeconds()).padStart(2, '0');\n\n    return `${hours}:${minutes}:${seconds}`;\n  }, [currentTime, offset]);\n\n  const hoursOnly = timeString.split(':')[0];\n  const minutesOnly = timeString.split(':')[1];\n  const secondsOnly = timeString.split(':')[2];\n\n  return (\n    <div className=\"space-y-4\">\n      <div className=\"bg-gray-900 rounded-xl p-6 border border-gray-700\">\n        <div className=\"font-mono text-5xl font-bold text-blue-400 tracking-wider text-center\" style={{\n          textShadow: '0 0 20px rgba(59, 130, 246, 0.5)',\n          fontFamily: '\"Courier New\", monospace',\n        }}>\n          {timeString}\n        </div>\n      </div>\n\n      <div className=\"grid grid-cols-3 gap-3\">\n        <div className=\"bg-gray-800 rounded-lg p-4 border border-gray-700 text-center\">\n          <div className=\"font-mono text-3xl font-bold text-green-400\">{hoursOnly}</div>\n          <div className=\"text-gray-500 text-xs mt-2 uppercase tracking-wider\">Hours</div>\n        </div>\n\n        <div className=\"bg-gray-800 rounded-lg p-4 border border-gray-700 text-center\">\n          <div className=\"font-mono text-3xl font-bold text-yellow-400\">{minutesOnly}</div>\n          <div className=\"text-gray-500 text-xs mt-2 uppercase tracking-wider\">Minutes</div>\n        </div>\n\n        <div className=\"bg-gray-800 rounded-lg p-4 border border-gray-700 text-center\">\n          <div className=\"font-mono text-3xl font-bold text-red-400\">{secondsOnly}</div>\n          <div className=\"text-gray-500 text-xs mt-2 uppercase tracking-wider\">Seconds</div>\n        </div>\n      </div>\n\n      <div className=\"text-center\">\n        <div className=\"inline-block bg-gray-800 rounded-lg px-4 py-2 border border-gray-700\">\n          <span className=\"text-gray-400 text-sm font-mono\">\n            {new Date(currentTime.getTime() + offset * 3600000).getUTCHours() >= 12\n              ? '🌙 PM'\n              : '☀️ AM'}\n          </span>\n        </div>\n      </div>\n    </div>\n  );\n}